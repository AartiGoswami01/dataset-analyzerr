services:
  dataset-analyzer:
    build: .
    container_name: dataset-analyzer
    environment:
      - OPENAI_API_KEY=your-openai-api-key-here
    stdin_open: true
    ports:
      - "5000:5000"
    tty: true
    networks:
      - agents-net

networks:
  agents-net: